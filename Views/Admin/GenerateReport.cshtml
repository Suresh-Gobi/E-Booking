@{
    ViewData["Title"] = "Generate Report";
}
<br/>
<h4>Report</h4>
<hr/>
<div class="row">
    <!-- Total Books Card -->
    <div class="col-md-3">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Total Books</h5>
                <p class="card-text">@ViewBag.TotalBooks</p>
            </div>
        </div>
    </div>

    <!-- Total Orders Card -->
    <div class="col-md-3">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Total Orders</h5>
                <p class="card-text">@ViewBag.TotalOrders</p>
            </div>
        </div>
    </div>

    <!-- Total Users Card -->
    <div class="col-md-3">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Total Users</h5>
                <p class="card-text">@ViewBag.TotalUsers</p>
            </div>
        </div>
    </div>

    <!-- Total Revenue Card -->
    <div class="col-md-3">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Total Revenue</h5>
                <p class="card-text">$1000</p> <!-- Static data, replace with actual total revenue -->
            </div>
        </div>
    </div>
</div>

<div class="row mt-3">
    <div class="col-md-6">
        <canvas id="totalChart" width="400" height="200"></canvas>
    </div>
    <div class="col-md-6">
        <canvas id="lineChart" width="400" height="200"></canvas>
    </div>
</div>

<div class="mt-3">
    <button class="btn btn-primary" id="downloadBtn">Download Report PDF</button>
</div>

@section Scripts {
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/3.7.0/chart.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.3.1/jspdf.umd.min.js"></script>
    <script>
        var ctxTotal = document.getElementById('totalChart').getContext('2d');
        var totalChart = new Chart(ctxTotal, {
            type: 'bar',
            data: {
                labels: ['Books', 'Orders', 'Users'],
                datasets: [{
                    label: 'Total Records',
                    data: [@ViewBag.TotalBooks, @ViewBag.TotalOrders, @ViewBag.TotalUsers],
                    backgroundColor: [
                        'rgba(255, 99, 132, 0.2)',
                        'rgba(54, 162, 235, 0.2)',
                        'rgba(255, 206, 86, 0.2)'
                    ],
                    borderColor: [
                        'rgba(255, 99, 132, 1)',
                        'rgba(54, 162, 235, 1)',
                        'rgba(255, 206, 86, 1)'
                    ],
                    borderWidth: 1
                }]
            },
            options: {
                scales: {
                    y: {
                        beginAtZero: true
                    }
                }
            }
        });

        var ctxLine = document.getElementById('lineChart').getContext('2d');
        var lineChart = new Chart(ctxLine, {
            type: 'line',
            data: {
                labels: ['January', 'February', 'March', 'April', 'May', 'June', 'July'],
                datasets: [{
                    label: 'Revenue',
                    data: [100, 200, 300, 400, 500, 600, 700], // Sample revenue data for each month
                    borderColor: 'rgba(75, 192, 192, 1)',
                    backgroundColor: 'rgba(75, 192, 192, 0.2)',
                    borderWidth: 1
                }]
            },
            options: {
                scales: {
                    y: {
                        beginAtZero: true
                    }
                }
            }
        });

        document.getElementById('downloadBtn').addEventListener('click', function () {
            var doc = new jsPDF();
            doc.text("Generate Report", 10, 10);
            // Add more content to the PDF as needed
            doc.save("report.pdf");
        });
    </script>
}
